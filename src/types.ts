export type CurrencyList = {
  currency: string
  label: string
  flagCode: string
}

export type CurrencyData = {
  label: string
  currency: string
  value: number
  rates: number
  flagCode: string
}